---
title: 练习(1) 正则表达式
description: Js习题记录
categories:
  - Js测试题
tags:
  - Javascript
  - codeWars
  - leetcode
date: 2018-05-08 16:25:25
---
> Your job is to write a function which increments a string, to create a new string. If the string already ends with a number, the number should be incremented by 1. If the string does not end with a number the number 1 should be appended to the new string.   
> 
> Examples:
> 
> foo -> foo1
> 
> foobar23 -> foobar24
> 
> foo0042 -> foo0043
> 
> foo9 -> foo10
> 
> foo099 -> foo100
> 
> Attention: If the number has leading zeros the amount of digits should be considered.


---

> 自己的解答(不推荐)

    function incrementString(str) {
    //开头有0 就在自增过后补0 没有0 就自增
        var _add = function (str) {
            var zeros = [];
            for (let i in str) {
                if (str[i] === "0") {
                    zeros.push(str[i])
                } else {
                    break
                }
            };
            if (zeros.length) {
                var nLength = String(Number(str) + 1).length,
                oLength =  String(Number(str)).length;
                //补0的时候注意进位
                if (nLength!=oLength||Number(str)===0) {
                //进位也应保持总位数不变(从数字0到数字1也属于进位，因为把最后一位算到开头了)
                    zeros.length -= 1;
                };
                return zeros.join("") + (Number(str) + 1)
            } else {
                return Number(str) + 1
            }
        }
        reg.test(str) ? str = str.replace(reg, function () {
         //判断字符串是否包含数字,有就将包含的数字自增后replace原来的,没有就直接自增。
            return _add(arguments[0])
        }) : str += 1;
        return str
    }


> 高票解答1
    
    function incrementString(input) {
        if(isNaN(parseInt(input[input.length - 1]))) return input + '1';
        return input.replace(/(0*)([0-9]+$)/, function(match, p1, p2) {
        //如图1所示的正则。第一组匹配0 第二组匹配尾部的0-9
          var up = parseInt(p2) + 1;
        //如果最后一位自增后需要进位就截掉开头数字0的最后一位。否则直接字符串拼接
          return up.toString().length > p2.length ? p1.slice(0, -1) + up : p1 + up;
        });
      }
      
![image](https://note.youdao.com/yws/public/resource/dcd2d86bcfd21e45851c01d8345fdada/xmlnote/6ACEC776E44B44A2837C5C96E4F566EE/1918)

图1
    
> 高票解答2(推荐)
> 
    function incrementString(input) {
    return input.replace(/([0-8]?)(9*)$/, function(s, d, ns) {
        //如图2所示的正则
        //一组匹配尾部的0-8 二组匹配尾部的9
        // 将第一组匹配到的自增 第二组全部换为0
        return +d + 1 + ns.replace(/9/g, '0');
        //当尾部存在9,9替换为0 尾部非9自增
        //当尾部不存在9,尾部自增
        //当尾部只有9,9替换成0 1和0做字符串相加
      });
     }
    
    
![image](https://note.youdao.com/yws/public/resource/dcd2d86bcfd21e45851c01d8345fdada/xmlnote/FCD334CB4D7B4CA2AE64777DC7699E67/1919)

图2

---
总结:主要考察的是正则的使用和+运算符。我没必要去遍历字符串一个个把0放数组里，用正则全部匹配，然后根据是否需要进位减位数即可